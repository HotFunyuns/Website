# Publishing queue report

_Generated by `npm run queue:report` on 2026-09-25 (America/Los_Angeles). Do not edit by hand; the workflow is described in [publishing-workflow.md](./publishing-workflow.md)._

## Rules in force

- Dates are calendar days in **America/Los_Angeles**.
- The hourly job releases from **09:00** onward, at most **1** article per day.
- Only `status: "scheduled"` articles with `editorialApproved: true` and a `publishAt` on or before today are eligible, earliest `publishAt` first, then by slug.
- `publishedAt` is stamped with the real release day. It is never set earlier, and no other article's dates are changed.
- Every article published after 2026-09-22 must carry `editorialApproved: true`; the build fails otherwise.

## Counts

| Status | Articles | Editorially approved |
| --- | --- | --- |
| draft | 0 | 0 |
| review | 0 | 0 |
| scheduled | 0 | 0 |
| published | 525 | 0 |

The 525 articles published on or before 2026-09-22 predate the approval flag. They are not marked approved, because nobody approved them individually, and they are not unpublished for having been released in bulk — weak or overlapping pages are handled through the refresh queue instead.

## Queue

The queue is empty. Nothing is scheduled, so the hourly job does nothing.

Projected dates assume one release per day and no failures. An approval, an edit, a manual release or a failed build moves them.

## Blocked

Nothing is blocked.

## Awaiting review or approval

No drafts or articles in review.

## Released through the queue

None yet.

## What happens next

The next run would do nothing: no scheduled, approved article is due.
